variant: fcos
version: 1.6.0
ignition:
  config:
    merge:
      - local: services/cockpit/cockpit.bu
      - local: services/tailscale/tailscale.bu
      - local: services/kinto/kinto.bu
passwd:
  users:
    - name: core
      password_hash: TODO_ENTER_HASH

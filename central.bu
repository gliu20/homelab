variant: fcos
version: 1.6.0
ignition:
  config: 
    merge:
      - local: cockpit.bu
passwd:
  users:
    - name: user
      password_hash: TODO_ENTER_HASH
      groups:
        - wheel
      shell: /bin/bash
